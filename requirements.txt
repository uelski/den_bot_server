langchain
langchain-qdrant
langchain-google-genai
langgraph
fastapi
uvicorn
bs4
langchain-community        # needed for many LC utilities
qdrant-client              # explicit client, langchain-qdrant depends on it
fastembed                  # FastEmbedSparse needs this explicitly
httpx                      # async HTTP for scraping agent
python-dotenv              # .env loading
pydantic[email]>=2.0       # FastAPI v2 uses pydantic v2; [email] adds EmailStr support for /feedback
sse-starlette              # streaming responses from FastAPI
shapely                    # polygon centroid computation (compute_neighborhood_centroids.py)
pandas                     # GTFS file joins (scripts/ingest_rtd_gtfs.py only — not loaded at runtime)
gtfs-realtime-bindings     # protobuf parser for app/tools/rtd_alerts.py
tavily-python>=0.5.0       # AsyncTavilyClient backing the search_denver_gov tool
cohere                     # rerank-english-v3.0 in app/graph/nodes/reranker.py (merges catalog + PDF KB hits)
trafilatura                # HTML main-content extraction for scripts/ingest_denvergov_pages.py (scripts only — not loaded at runtime)
langgraph-checkpoint-redis>=0.4.0  # AsyncRedisSaver for multi-turn conversation memory
google-cloud-storage       # v4 signed-URL generation for the /admin PDF upload flow (app/admin.py)
google-auth                # IAM signBlob keyless-signing path in app/admin.py (imported directly)
dotenv
fastembed

# Recommended for robustness
tenacity                   # retry logic for LLM/scraping calls
structlog

# Testing
pytest
pytest-asyncio

